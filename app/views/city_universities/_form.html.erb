<%= form_for(city_university) do |f| %>
  <% if city_university.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city_university.errors.count, "error") %> prohibited this city_university from being saved:</h2>

      <ul>
      <% city_university.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
